// just the ForgeCard Object

export default class ForgeCard {
    constructor(id, name, icon, quantity) {
        this.id = id
        this.name = name;
        this.icon = icon;
        this.quantity = quantity;
    }
}